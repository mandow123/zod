// The primary market entry is the complete 100-supplier inquiry directory.
// Reservation remains available as a separate tab, but must not make the
// market appear empty when its live catalog is temporarily unavailable.
export const FORMAL_MARKET_FRESH_SECTION = '算力租用' as const;
